package com.saucedemo.listeners;

import org.testng.ITestListener;

public class AllureListener implements ITestListener {
    // Hemos movido la lógica al BaseTest para mayor fiabilidad en ejecuciones individuales
}
